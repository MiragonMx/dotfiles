[user]
	name = MiragonMx
	email = 80544476+MiragonMx@users.noreply.github.com
[gui]
	recentrepo =
[init]
	defaultBranch = main
[push]
	default = current

[core]
    pager = delta

[interactive]
    diffFilter = delta --color-only

[delta]
    navigate = true  # use n and N to move between diff sections
    dark = true      # or light = true, or omit for auto-detection

[merge]
    conflictstyle = zdiff3
